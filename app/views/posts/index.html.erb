< br >  < br >  < p id = "notice" ><%= notice %></p >  < h1 class = "mt-5 text-center" > Les dernières actualités !</h1 >  < table >  < tbody >  < div class = "row" >  < div class = "col-md-3 " >  <%= image_tag(current_user.avatar) %>  < h3 class = "text-center mt-5" ><%= link_to "#{current_user.user_name}", user_path(current_user) %></h3 >  < h3 class = "mt-5 text-center" > Liste de mes articles </h3 >  <% current_user.posts.each do | post |  %>  < p class = "text-center" ><%= post.title %></p >  <% end %>  < h3 class = "mt-5 text-center" > Vos categories </h3 >  <% current_user.categories.each do | category |  %>  < p class = "text-center" ><%= category.name %></p >  <% end %>  < h5 class = "text-center" ><%= link_to 'Partager vos connaissances !', new_post_path %></h5 >  </div >  < div class = "container col-md-6" >  < div class = "container col-md-10" >  <%= form_for @post, remote:true do | f |  %>  < div class = "form-group" >  <%= f.label:title, 'Titre' %>< br />  <%= f.text_field:title, autofocus:true, class:"form-control" %>  </div >  < div class = "form-group" >  <%= f.label:content, 'Contenu' %>< br />  <%= f.text_area:content, autofocus:true, class:"form-control" %>  </div >  < div class = "field" >  <%= f.label:category %>  <%= select_tag(:category, options_for_select(@categories.collect { | c | [c.name, c.id] })) %>  </div >  <%= f.submit %>  <% end %>  </div >  < section id = 'sectionposts' >  <%= render @posts %>  </section >  </div >  < div class = "col-md-3" >  < h5 > Envie d'échanger?</h5>
        <h3 class="card-title">
          Ajouter liste des users pour messagerie
        </h3>
      </div>
    </div>

  </tbody>

</table>    






